# 2026-06-25 12:45 +03:00 - Codex - план привлечения подписчиков `ИИ в дело`

## Исходный запрос

Пользователь попросил продолжить привлекать подписчиков в Telegram-канал `ИИ в дело`.

## Контекст

- Канал `ИИ в дело` открыт в Telegram Web, видно 2 подписчика.
- Пост 05 опубликован ранее 2026-06-25.
- Ранее работали/проверялись площадки: `@businessclass_rbc`, `@cyber_misha`, `@nata_progrevy`.
- `@sam_delo` и `@mimimarketing` ранее были заблокированы через `APPLY TO JOIN GROUP`.
- `@delohub` не показал открытого обсуждения.

## Что сделано

- Прочитан Telegram workflow router и browser workflow.
- Выполнен Aion SML bootstrap.
- Проверен unrelated active run gate: `trading_mvp` остается `RUNNING`, действий по нему не выполнялось.
- Проверен канал `ИИ в дело` в Telegram Web: видно 2 подписчика.
- Подготовлен рабочий файл `C:\Users\koval\Documents\ТГ БОТ\subscriber-acquisition-2026-06-25.md`.
- В файл добавлены:
  - рекомендуемый bio личного Telegram для конверсии в канал;
  - комментарий-кандидат для `@cyber_misha`;
  - комментарий-кандидат для `@oqode`;
  - универсальный ответ для живого вопроса в `@businessclass_rbc`;
  - список площадок, куда сейчас не тратить время.

## Проверки

- Файл создан и содержит блоки `@iivdelo_ai`, `@cyber_misha`, `@oqode`, `@businessclass_rbc`.
- `git status` в текущей Codex-сессии нестабилен при bare `git`; команда проходит при явном префиксе:

```powershell
$env:PATH = 'C:\Program Files\Git\cmd;' + $env:PATH
git -C 'C:\Users\koval\Documents\ТГ БОТ' status --short
```

## Риски и ограничения

- Не менял Telegram bio без отдельного подтверждения.
- Не публиковал комментарии без отдельного подтверждения точного текста и места.
- Прямой hash-переход `web.telegram.org/a/#@username` в текущем Telegram Web вел себя нестабильно; для публикации лучше открывать целевой пост/обсуждение через видимый Telegram Web и проверять контекст перед отправкой.

## Следующий шаг

Получить подтверждение пользователя на:

1. изменение bio личного Telegram на `ИИ и Telegram-автоматизация для малого бизнеса. Канал: @iivdelo_ai`;
2. публикацию первого комментария без ссылки в `@cyber_misha` под постом про ИИ-персонализацию.

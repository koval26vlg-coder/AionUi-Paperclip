# Запрос

Пользователь спросил, почему `AI100` по Максиму Волынкину была пустой, и показал Bitrix-скрин с фильтром `Вчера`, где есть сделки и отправленные счета.

# Результат

- Объяснено смещение дат: `AI100` была за `2026-06-23`, а скрин `Вчера` на `2026-06-25` показывает `2026-06-24`.
- Запущен automation-3 за текущий день: `run_and_write_daily.ps1 -RunDate 2026-06-25`.
- Target: `2026-06-24`, лист `Июнь 2026 ОП`, колонка `AJ`.
- Для Максима Волынкина записано `AJ100=60%` (`5` новых сделок, `3` дошли до `Счет отправлен`).
- Остальные значения: `AJ18=50%`, `AJ34=0%`, `AJ50=33%`, `AJ66=33%`, `AJ84=60%`, `AJ116=50%`, `AJ132=80%`.

# Проверки

- SML ping/startup/semantic ok.
- Writer readback подтвердил все ячейки, `updated_cells=8`.
- Artifact: `C:\Users\koval\Documents\ОК.ру\exports\sheets\automation-3-conversion-invoices-2026-06-25.json`.

# Риски

- Риск путаницы `Вчера` в Bitrix: относительная дата зависит от даты просмотра, а колонка Google Sheet привязана к абсолютной целевой дате.

# Следующему

- При вопросах о расхождениях первым делом сопоставлять абсолютную дату UI-фильтра и колонку Sheet.

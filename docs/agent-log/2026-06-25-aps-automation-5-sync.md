# Запрос

Зафиксировать результат ежедневной синхронизации АПС `automation-5` Bitrix24 -> Google Sheets за 2026-06-25.

# Результат

АПС запущена, так как 2026-06-25 — четверг и не первый рабочий день недели. SML и preflight прошли успешно. Перед запуском обнаружено, что результат 2026-06-24 был записан в state, но не в `docs/agent-log`; создан восстановительный локальный лог.

Основной write helper `C:\Users\koval\Documents\ОК.ру\automations\aps\run_sync_with_env.ps1 -Write`: `since_utc=2026-06-24T06:23:14+00:00`, fetched=360, matched=67, inserted=7, updated=60, deleted=11. Финальный dry-run: inserted=0, deleted=0.

Финальная таблица: 158 строк, 158 уникальных ID, дубли=0, запрещенные менеджеры=0, нарушения воронки=0, нарушения стадии=0, строки к удалению=0, бизнес-расхождения=0. Последние 7 добавленных строк имеют заполненный `Общий смысл`.

# Проверки

Compact audit проверил видимый `Менеджер`, текущего ответственного Bitrix, `CATEGORY_ID`, текущую стадию, missing deals и бизнес-поля. Осталось 22 format-only отличия по суммам из-за пробелов-разделителей.

# Риски

Нужен штатный `--diff-report` и diff-only обновление, чтобы не считать overlap как бизнес-обновления. Gate `trading_mvp` остается RUNNING, но АПС не трогала trading-процессы.

# Следующему

Следующий запуск: SML -> preflight -> write helper -> dry-run -> compact audit. Не запускать в первый рабочий день недели.

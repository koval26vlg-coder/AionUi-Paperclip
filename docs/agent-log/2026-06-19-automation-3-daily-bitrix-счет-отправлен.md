# Запрос

automation-3: daily Bitrix `Счет отправлен, %` -> Google Sheet за предыдущий рабочий день.

# Результат

2026-06-19 выполнено: target `2026-06-18`, лист `Июнь 2026 ОП`, колонка `AD`. Записано и проверено: `AD15=75%`, `AD29=75%`, `AD45=83%`, `AD61=33%`, `AD78=60%`, `AD93=40%`, `AD108=83%`, `AD123=40%`.

Расчет шел по cohort-формуле: `38` новых сделок `CATEGORY_ID=1`, `24` дошли до `C1:UC_9NU15J`; `1` stage-history переход вне периода создания отсечен. Все новые сделки попали в текущий набор восьми строк менеджеров.

# Проверки

SML ping/startup/semantic ok. Preflight ok=true. Calendar writer: run `2026-06-19` в колонке `AE`, target `2026-06-18` в колонке `AD`. Service-account writer обновил `8` ячеек и контрольное чтение подтвердило значения. `py_compile` для `calc_invoice_conversion.py` и `write_daily.py` ok.

# Риски

Старые июньские daily-колонки до исправления cohort-формулы могут быть завышены; нужен ретро-аудит. Writer пока опирается на статические июньские строки метрики `15, 29, 45, 61, 78, 93, 108, 123`.

# Следующему

Продолжать основной маршрут через `write_daily.py --run-date`, `run_daily.ps1 -TargetDate`, `write_daily.py --write`. Добавить единый wrapper, чтобы preflight и writer всегда работали в одном PowerShell-процессе и не теряли env.

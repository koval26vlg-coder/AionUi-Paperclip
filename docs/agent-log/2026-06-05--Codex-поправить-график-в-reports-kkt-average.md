# Codex — 2026-06-05

## Запрос
Поправить график в `reports/kkt-average-check-2026-01-05.xlsx`: добавить числа и исправить отображение января.

## План
SML -> inspect chart -> openpyxl recreate -> direct chart XML patch -> Excel COM validate -> formula scan -> log.

## Результат
На листе `Свод_месяцы` пересоздана/исправлена диаграмма: одна серия `Средний чек`, легенда отключена, ось X = `B2:B6` (`Январь 2026`...`Май 2026`), значения = `E2:E6`, подписи значений включены с форматом `# ##0`. В XML диаграммы включены `catAx` и `valAx` (`delete=0`).

## Изменённые файлы
- reports/kkt-average-check-2026-01-05.xlsx
- docs/agent-log/2026-06-05-fix-kkt-average-check-chart.md

## Риски и ограничения
Изменялась только диаграмма `xl/charts/chart1.xml`; расчетные данные и формулы не менялись.

## Что следующему агенту
Если график пересоздается через openpyxl, проверять, что оси не получают `delete=1`, иначе месяцы снова скроются.

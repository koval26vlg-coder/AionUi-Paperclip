# Ревью анализа Codex по падению ws_collect_72h_sweep_visible_20260702_101730

Дата: 2026-07-03 ~14:00 +03
Агент: Claude Code (ревью по запросу пользователя)

## Вердикт по анализу Codex

Согласен по существу со всеми 6 рекомендациями (durable collect protocol, periodic partial manifest, child-owned manifest, heartbeat + collector_exit_reason, verification run 2-3ч перед 72ч, запрет replay/grid по partial). Диагноз сходится с пунктом A1 аудита Claude Code 2026-07-02 — два независимых ревью согласованы. Отдельно ценная находка Codex: footgun `_latest_ws_input()` в ws-postprocess (без явного manifest может взять старый датасет).

## Новые факты диагностики (дополнение к анализу Codex)

1. Все три WS-потока (gateio, mexc 01of02, mexc 02of02) перестали писать **одновременно в окне 18 секунд**: LastWriteTime 12:07:57 / 12:08:14 / 12:08:15 (02.07.2026), после ~1ч50м работы (старт 10:18:31).
2. Системный журнал Windows 11:55–12:20: **нет событий сна/перезагрузки/shutdown** (Id 41/42/107/1074/6008 отсутствуют). Машина работала.
3. Сетевые обрывы исключаются: reconnect-цикл пишет control-события (`connect_attempt_error`) прямо в raw; raw замолчал полностью.
4. Вывод: **процесс-дерево убито извне при живой сети и живом хосте** — наиболее вероятно закрытие видимого окна терминала (Windows убивает дерево) или внешний kill. Monitor PID в gate переиспользован — монитор умер вместе с ребёнком.

## Дополнения к рекомендациям

1. **Отвязать жизнь коллектора от окна**: collector — detached-процесс, сам владеет manifest/heartbeat; «видимое окно» — отдельный наблюдатель (tail state-файла). Иначе durable protocol будет лишь документировать очередную смерть, а не предотвращать.
2. **Сегментация (3ч) ценнее periodic manifest**: завершённый сегмент — самостоятельная ценность (полный manifest), смерть рана = частичный успех, а не потеря; periodic manifest оставить как диагностику внутри сегмента.
3. Постфактум-финализация: команда finalize, собирающая stitched manifest из сегментов даже после смерти рана — gate закрывается штатно, а не ручной правкой.
4. Стратегически: WS-ветка обслуживает слабейший prior (spot sweep/reversal); carry/momentum promising и копят forward автоматически — hardening делать, 72ч ре-раны не форсировать до verification run.

## Решение пользователя

Пользователь одобрил реализацию пунктов 1–3 (detached+child-owned manifest, сегментация+stitching, фикс footgun) — выполняет Claude Code, см. следующую запись журнала.

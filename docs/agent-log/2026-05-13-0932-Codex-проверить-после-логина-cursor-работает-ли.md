# Codex — 2026-05-13T09:32:29.367Z

## Запрос
Проверить после логина Cursor, работает ли общая память SML для Cursor/Kiro/Codex

## План
Проверить Cursor Agent auth/MCP, исправить allowlist, выполнить Cursor smoke, проверить Kiro логи, исправить MCP tools/call формат для строгих клиентов, прогнать тесты.

## Результат
Cursor Agent залогинен, сервер sml ready, 10 инструментов доступны. В Cursor CLI добавлено разрешение Mcp(sml:*), после чего Cursor успешно вызвал sml.ping/startup_pack/semantic_query/add_log. MCP adapter теперь возвращает стандартный result.content и сохраняет старые поля payload; полный набор тестов tools/sml/tests зелёный: 142 passed. Kiro по логам видит mcp_sml_* инструменты и ранее подключался к sml, но его облачный агент сейчас падает с Service temporarily unavailable/high traffic, не из-за SML.

## Изменённые файлы
- C:/Users/koval/.cursor/cli-config.json
- D:/AionUi-Paperclip/tools/sml/mcp_adapter.py

## Риски и ограничения
Текущая Codex-сессия потеряла старое MCP-подключение после перезапуска процесса; новый Codex/Cursor/Kiro запуск поднимет исправленный адаптер. Для Kiro нужен Reload Window/перезапуск, чтобы он поднял новый SML MCP-процесс.

## Что следующему агенту
Перезапустить окно Kiro и при необходимости текущую сессию Codex, затем выполнить ручной Kiro smoke: sml.ping, sml.startup_pack, sml.semantic_query, sml.add_log.

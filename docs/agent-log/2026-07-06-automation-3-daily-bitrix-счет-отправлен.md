# Запрос

automation-3: ежедневный перенос Bitrix `Счет отправлен, %` в Google Sheet `Дашборд ОП` по менеджерам за предыдущий рабочий день. Запуск 2026-07-06.

# Результат

Preflight `ok=true`; SML MCP доступен. Обнаружено, что artifact за `2026-07-03` отсутствовал, поэтому сначала дозакрыт пропущенный запуск:

- Run `2026-07-03`, target `2026-07-02`, лист `Июль 2026 ОП`, колонка `N`: `N18=67%`, `N34=20%`, `N50=50%`, `N66=50%`, `N84=50%`, `N100=100%`, `N116=60%`, `N132=33%`.
- Run `2026-07-06`, target `2026-07-03`, лист `Июль 2026 ОП`, колонка `O`: `O18=33%`, `O34=100%`, `O50=25%`, `O66=67%`, `O84=29%`, `O100=40%`, `O116=60%`, `O132=60%`.

# Проверки

Оба запуска выполнены через `C:/Users/koval/Documents/ОК.ру/automations/conversion_invoices/run_and_write_daily.ps1`. В обоих случаях `updated_cells=8`, контрольное чтение writer совпало, `unknown_assigned_by=[]`.

# Риски

Aion file watcher stale; SML MCP работал. Активный gate `trading_mvp` был `RUNNING`, но по нему действия не выполнялись.

# Следующему

Для automation-3 перед текущим запуском проверять наличие artifact за предыдущий рабочий heartbeat. Если artifact отсутствует, сначала бэкфилл этой run-date, затем текущий запуск. Writer должен выбирать месячный лист по metadata (`Июль 2026 ОП` для июля), не использовать старый single-sheet fallback как основной маршрут.

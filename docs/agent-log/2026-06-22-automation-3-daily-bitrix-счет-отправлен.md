# Запрос

automation-3: daily Bitrix `Счет отправлен, %` -> Google Sheet за предыдущий рабочий день.

# Результат

2026-06-22 выполнено: target `2026-06-19`, лист `Июнь 2026 ОП`, колонка `AE`. После исправления динамического поиска строк записано и проверено: `AE18=33%`, `AE34=0%`, `AE50=0%`, `AE66=25%`, `AE84=50%`, `AE100=40%`, `AE116=100%`, `AE132=67%`.

Расчет шел по cohort-формуле: `27` новых сделок `CATEGORY_ID=1`, `9` дошли до `C1:UC_9NU15J`; `3` stage-history перехода вне периода создания отсечены. Все новые сделки попали в текущий набор восьми строк менеджеров.

# Проверки

SML ping/startup/semantic ok. Preflight ok=true. Calendar writer: run `2026-06-22` в колонке `AH`, target `2026-06-19` в колонке `AE`. Service-account writer обновил `8` реальных ячеек строки `Счет отправлен, %` и контрольное чтение подтвердило значения. `py_compile` для `calc_invoice_conversion.py` и `write_daily.py` ok.

# Риски

Ретро-аудит выполнен отдельной записью `2026-06-22-automation-3-retro-audit.md`. Старые статические строки `15, 29, 45, 61, 78, 93, 108, 123` больше не использовать.

# Следующему

Продолжать основной маршрут через `run_and_write_daily.ps1`.

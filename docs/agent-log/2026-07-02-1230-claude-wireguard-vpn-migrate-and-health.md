# 2026-07-02 12:30 Claude Code — Перенос VPN-проекта и проверка работоспособности

## Исходный запрос

Пользователь: проект по VPN был перенесён. Основное место работы —
`C:\Users\koval\Documents\WireGuard VPN`. Изначально создавался в
`C:\Users\koval\Documents\ZolotyayLopata`. Задача: перенести проект полностью
в `WireGuard VPN`, зачистить из `ZolotyayLopata` всё лишнее, проверить
работоспособность, откалибровать, скорректировать и запустить VPN для работы.

## Краткий план

- Aion/SML bootstrap по теме VPN.
- Сравнить обе директории, перенести VPN-файлы по allow-list из migrate_vpn.ps1.
- Слить tools\vpn (в назначении уже были 3 файла), удалить идентичные дубли.
- Зачистить ZolotyayLopata от VPN-объектов, оставить только trading_mvp.
- Прогнать health check, серверную SSH-проверку, e2e-тест туннеля.

## Что сделано

### Перенос (завершён)
- Перенесены все объекты VPN из allow-list migrate_vpn.ps1: 30 корневых .cmd,
  vpn-control-panel.config.json, 8 папок *-out, docs\udp-over-ssh-runbook.md,
  сам migrate_vpn.ps1. Итого 41 корневой объект.
- tools\vpn слит: 103 файла перенесены, 2 идентичных дубля
  (deploy_android_xray_reality.ps1, watch_android_xray_reality.ps1) удалены,
  __pycache__ удалён. Пустая tools\vpn в источнике удалена.
- В ZolotyayLopata из VPN-объектов не осталось ничего (проверено grep по
  vpn|wireguard|xray|singbox|openvpn|ikev2|ssh-tun вне .git). Остался только
  торговый проект: trading_mvp, TRADING_*.cmd, docs, exports, tools\*.ps1.
- git ZolotyayLopata: VPN-файлы были untracked (в .gitignore или не добавлены),
  поэтому удалений отслеживаемых файлов нет — история торгового проекта не задета.

### Калибровка/корректировка
- check-vpn-health.ps1: исправлен баг серверной SSH-проверки — here-string с
  `\$(date -Is)` и `\$s` ломался в PowerShell (parameter 'Is'). Заменил на `` `$ ``.
- ANDROID_WIREGUARD_CHECKLIST_RU.md: пути ZolotyayLopata -> WireGuard VPN.
- Добавлен .gitignore (исключает *-out с ключами/конфигами/браузер-профилями,
  exports, __pycache__, *.log).
- Первый коммит в новом репозитории: 142 файла (root-commit 35fcea0).

### Проверка работоспособности — СЕРВЕР ЗДОРОВ (важное отличие от 06-30)
- Внешний check-host.net: 443/tcp доступен с 5/5 узлов, 22/tcp с 5/5 узлов.
  (30 июня оба порта давали success_count=0 из 5 — сервер выглядел мёртвым.)
- SSH read-only: xray active + enabled, слушает *:443 (pid=627),
  `xray run -test` => Configuration OK. Warnings за 15 мин — нет.
- REALITY TLS-хендшейк на 443 с SNI www.microsoft.com => TLS1.3,
  валидный сертификат CN=www.microsoft.com (маскировка работает).
- Учётные данные Desktop-ссылки полностью совпадают с сервером
  (сравнение по усечённым SHA-256, приватный ключ не покидал VPS):
  uuid_match=True, pbk_match=True, sid_match=True, sni_match=True.

### Локальный e2e-тест — заблокирован Happ (не дефект)
- Тестовый Xray-клиент (SOCKS 127.0.0.1:19181) к api.ipify.org через туннель:
  дозвон к 147.90.11.165:443 виснет, curl exit 28 (timeout).
- Причина подтверждена: `Find-NetRoute 147.90.11.165` => интерфейс happ-tun,
  next hop 172.18.0.2. Default route тоже через happ-tun (metric 0). Мой клиент
  попадает в туннель Happ, петля/конфликт. Прямой TLS-connect проходит, но
  вложенный REALITY-хендшейк через второй клиент — нет.
- Windows сейчас выходит через Happ (внешний IP 50.7.88.214), т.е. Happ активен,
  но подключён НЕ к ZL-REALITY-443 (иначе IP был бы 147.90.11.165).

## Файлы изменены

- Новый репозиторий `C:\Users\koval\Documents\WireGuard VPN`: перенесены VPN-файлы,
  добавлен .gitignore, исправлены check-vpn-health.ps1 и
  tools\vpn\ANDROID_WIREGUARD_CHECKLIST_RU.md, сделан root-commit 35fcea0.
- Из `C:\Users\koval\Documents\ZolotyayLopata` удалены все VPN-объекты.
- Этот журнал.

## Проверки

- Aion bootstrap выполнен.
- Перенос проверен: diff обеих директорий, хэши дублей, grep-остатки, git status.
- check-vpn-health.ps1 -External и -Server отработали.
- Внешние TCP (check-host), SSH, REALITY TLS, сверка кредов — все пройдены.

## Риски и ограничения

- Server-side WireGuard `wg-quick@wg0` на VPS всё ещё active (легаси-ветка,
  порту xray 443 не мешает, но это неубранный слушатель).
- Полный локальный e2e через Windows нельзя пройти, пока Happ владеет маршрутом
  по умолчанию; нужен либо временный host-route 147.90.11.165 через физический
  gateway (admin), либо временная остановка Happ (роняет текущий выход).
- «Запуск для работы»: сервер готов и доступен. На Windows клиент — Happ, его
  надо переключить на профиль ZL-REALITY-443; основной клиент по README — Android
  (v2rayNG/Hiddify), профиль ZL-REALITY-443, критерий ipify => 147.90.11.165.

## Что должен проверить следующий агент

- При явном разрешении: временно остановить Happ или добавить host-route и
  прогнать локальный e2e (ожидаемый ipify = 147.90.11.165).
- Решить судьбу server-side wg-quick@wg0 (нужен ли WireGuard как резерв).
- На Android включить ZL-REALITY-443, открыть https://api.ipify.org => 147.90.11.165.

---

## Дополнение (продолжение сессии): углублённая диагностика туннеля

Пользователь попросил не рвать связь (Happ должен оставаться поднятым) и проверять
синтетически. По его явному согласию был один цикл stop-Happ → тест → возврат Happ,
после чего Happ полностью восстановлен (happ-tun Up, выход 50.7.88.x). Дальше — только
синтетика без остановки Happ.

### Что выяснено про сам VLESS+REALITY туннель (147.90.11.165, ZL-REALITY-443)
- Внешняя доступность 443/tcp: 40/40 узлов check-host, включая RU (ru2/ru3 31-41ms),
  UA, MD. Блокировки на стороне сервера или «по всей РФ» НЕТ.
- Параметры REALITY на сервере ПОЛНОСТЬЮ совпадают со ссылкой (сверено открыто):
  pbk=Y6lGdxqwvCv_X4hTTZsVP7dkig6O2d6xrn1QO9TzV0g, sid=7541e1d4940e727d,
  sni=www.microsoft.com, flow=xtls-rprx-vision, 1 client, UUID_MATCH=True.
  dest=www.microsoft.com:443 доступен с сервера (HTTP 200, 0.24s).
- Рассинхрона «конфиг правили, сервис не рестартили» НЕТ: config.json mtime 2026-06-25,
  xray стартовал 2026-07-01 15:34 UTC (процесс загрузил актуальный конфиг).
- НО: ни один синтетический VLESS-клиент не смог протуннелировать трафик:
  - с Windows напрямую (Happ off): TCP до сервера таймаутил с домашнего IP;
  - с Windows через Happ: дозвон висит (петля через happ-tun);
  - на самом сервере, self-dial на публичный 147.90.11.165:443: EOF;
  - на самом сервере, чистый 127.0.0.1:443: EOF;
  - СВЕЖАЯ REALITY-пара (новые ключи) на запасном порту 8443 + клиент: тоже нет ответа.
- Поскольку даже свежая заведомо-корректная пара не туннелирует в моих тестах, вывод
  «профиль ZL сломан / ключи разошлись» НЕ доказан. Наиболее вероятно одно из двух:
  (1) системная особенность сервера (сборка xray 26.3.27 REALITY / iptables /
      активный wg-quick@wg0 / XTLS-Vision через loopback), либо
  (2) моя методика синтетического теста через transient `xray run -c` клиент даёт
      стабильный ложный отрицательный результат для Vision (Vision-over-loopback
      известен своей капризностью), а реальные клиенты (v2rayNG/Hiddify) работают.
- Синтетикой различить (1) и (2) больше нельзя. Нужен РЕАЛЬНЫЙ клиент.

### Уборка
- Все временные конфиги/логи на сервере (/tmp/zl_*) и локальный scratchpad удалены.
- Продакшн xray на 443 не тронут, активен, 1 листенер. Happ у пользователя работает.

### Рекомендация следующему агенту / пользователю
1. РЕАЛЬНЫЙ тест: на телефоне включить ZL-REALITY-443 (v2rayNG/Hiddify) по МОБИЛЬНОМУ
   интернету (не домашний Wi-Fi), открыть https://api.ipify.org, ждать 147.90.11.165.
   Это отделит «сервер ок, method artifact» от «сервер реально не туннелирует».
2. Если реальный клиент ТОЖЕ не грузит: делать server-side repair — либо
   tools/vpn/vps_repair_xray_reality.sh, либо полный redeploy
   tools/vpn/deploy_android_xray_reality.ps1 (регенерит ключи+link+QR, рестарт xray).
   Это меняет серверный конфиг и ссылку/QR — только с согласия пользователя.
3. Отдельно рассмотреть смену REALITY dest/SNI с www.microsoft.com на более
   стабильный домен, если repair не поможет.

---

## КОРНЕВАЯ ПРИЧИНА НАЙДЕНА: авария инфраструктуры HOSTKEY

Пользователь сообщил официальное уведомление HOSTKEY: идёт ПОЭТАПНОЕ восстановление
серверного оборудования и инфраструктуры, восстановлено ~80% серверов; если сервер
недоступен — зайти в ЛК https://invapi.hostkey.ru/, нажать Power On, дождаться загрузки,
иначе обратиться в поддержку.

Это полностью объясняет все наблюдения и снимает подозрения с конфига/клиента:
- 30.06 Codex: сервер снаружи полностью недоступен (22 и 443 таймаут со всех узлов) —
  совпадает с пиком аварии.
- Сейчас: xray стартовал 01.07 15:34 UTC (сразу после частичного восстановления),
  SSH/TCP/TLS до сервера проходят — сервер «поднялся» на L3/L4.
- НО VLESS+REALITY туннель не несёт трафик даже с самого сервера (loopback, свежие ключи).
- Реальный GUI-клиент Happ, добавленный в этой сессии (профиль ZL-REALITY-443,
  VLESS/TCP/REALITY), к серверу ТОЖЕ не подключился — подтверждает, что дело не в моей
  тестовой методике и не в профиле, а в состоянии сервера/ноды при частичном восстановлении.
- Конфиг сервера и ссылка проверены — корректны и совпадают. Регенерация ключей/redeploy
  НЕ требуется и проблему не решит, пока инфраструктура HOSTKEY не восстановлена полностью.

## Итоговое состояние сессии
- Перенос проекта ZolotyayLopata -> WireGuard VPN: завершён, 2 коммита, ZL вычищен.
- Калибровка: check-vpn-health.ps1 (SSH-баг), пути в доке, .gitignore — исправлено.
- Профиль ZL-REALITY-443 ДОБАВЛЕН в Happ (готов к подключению, когда сервер вернётся).
- Интернет пользователя стабилен через Horizon/Germany (Happ), не прерывался.

## Действия пользователю (сторона HOSTKEY)
1. ЛК HOSTKEY https://invapi.hostkey.ru/ -> проверить статус сервера 147.90.11.165;
   при необходимости Power On, дождаться загрузки. (Агент в ЛК не заходит — это аккаунт/пароли.)
2. Сервер отвечает по SSH (значит включён) — вероятнее, нода/инфра ещё восстанавливается;
   можно перезагрузить через ЛК ИЛИ дождаться полного восстановления. Reboot по SSH во
   время активного staged-восстановления НЕ рекомендуется (риск не подняться).
3. После восстановления: в Happ подключить ZL-REALITY-443 -> https://api.ipify.org ->
   ждать 147.90.11.165. Либо повторно прогнать tools/check-vpn-health.ps1.
4. Если после полного восстановления не работает — тикет в поддержку HOSTKEY с пометкой,
   что клиентский конфиг проверен и корректен (проблема на стороне сервера/инфраструктуры).

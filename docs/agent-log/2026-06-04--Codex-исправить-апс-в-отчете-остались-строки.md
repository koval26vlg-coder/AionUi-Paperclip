# Codex — 2026-06-04

## Запрос
Исправить АПС: в отчете остались строки с запрещенными менеджерами после cleanup.

## План
SML -> Sheets search -> patch cleanup -> retry DNS -> write -> verify -> log.

## Результат
Найден дефект: cleanup смотрел текущего ответственного Bitrix, но не видимую колонку `Менеджер` в таблице. Добавлена проверка sheet manager, retry для OAuth DNS, sheet.max_attempts=5. Write: deleted=21. Проверка Sheets: запрещенных менеджеров 0. Финальный dry-run: deleted=0.

## Изменённые файлы
- automations/aps/sync_bitrix_to_sheet.py
- automations/aps/config.json
- docs/agent-log/2026-06-04-aps-manager-cleanup-fix.md

## Риски и ограничения
3 ID из списка остались, но уже с разрешенными менеджерами; если нужно исключать именно сделки, нужен excluded_deal_ids. DNS нестабилен.

## Что следующему агенту
При новом случае искать по колонке `Менеджер`; при необходимости добавить blacklist по deal_id.

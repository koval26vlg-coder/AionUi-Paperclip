# Запрос

Зафиксировать результат ежедневной синхронизации АПС `automation-5` Bitrix24 -> Google Sheets за 2026-06-19.

# Результат

АПС запущена, так как 2026-06-19 — пятница и не первый рабочий день недели. Основной write helper сначала прочитал 322 сделки, нашел 74 подходящие, добавил 6 строк и обновил 68. Контрольный аудит выявил, что существующие строки вне инкрементального окна не обновлялись по текущему менеджеру/стадии и не удалялись при уходе из разрешенных стадий ОП. Helper в `C:\Users\koval\Documents\ОК.ру\automations\aps\sync_bitrix_to_sheet.py` исправлен: cleanup теперь проверяет текущую стадию ОП, критерии попадания и базовые бизнес-поля; `Общий смысл` для старых строк сохраняется, если сделка не попала в окно изменений.

После патча корректирующий write прочитал 33 сделки, обновил 88 строк и удалил 33 лишние строки. Итоговая таблица: 163 строки, дублей 0, запрещенных менеджеров 0, нарушений воронки 0, нарушений стадии 0, бизнес-расхождений 0.

# Проверки

`py_compile` прошел. Финальный dry-run: добавлено 0, удалено 0. Финальный аудит показал только 26 format-only отличий по суммам из-за пробелов-разделителей Google Sheets.

# Риски

Google Sheets один раз вернул `WinError 10060`; нужен штатный retry. Helper все еще обновляет строки инкрементального окна без точного diff ячеек.

# Следующему

Добавить `--diff-report`, закрепить retry для `WinError 10060`, не обновлять `Обновлено` без бизнес-изменений, оставить правило пропуска первого рабочего дня недели.

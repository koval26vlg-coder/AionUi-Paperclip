# Запрос

Зафиксировать результат ежедневной синхронизации АПС `automation-5` Bitrix24 -> Google Sheets за 2026-06-23.

# Результат

АПС запущена, так как 2026-06-23 — вторник и не первый рабочий день недели. SML и preflight прошли успешно. Основной write helper `C:\Users\koval\Documents\ОК.ру\automations\aps\run_sync_with_env.ps1 -Write`: `since_utc=2026-06-19T06:35:33+00:00`, fetched=419, matched=82, inserted=14, updated=68, deleted=10.

После основного write dry-run показал inserted=0/deleted=0, но независимый аудит обнаружил сделку `135609`, которая уже после записи перешла в `Сделка проиграна`. Выполнен короткий cleanup-write: fetched=11, matched=3, inserted=0, updated=3, deleted=1. Финальный dry-run: inserted=0/deleted=0.

Финальная таблица: 166 строк, 166 уникальных ID, дубли=0, запрещенные менеджеры=0, нарушения воронки=0, нарушения стадии=0, строки к удалению=0, бизнес-расхождения=0. Последние 14 добавленных строк имеют заполненный `Общий смысл`.

# Проверки

Финальный compact audit проверил видимый `Менеджер`, текущего ответственного Bitrix, `CATEGORY_ID`, текущую стадию, missing deals и бизнес-поля. Осталось 23 format-only отличия по суммам из-за пробелов-разделителей.

# Риски

Сделки могут менять стадию во время выполнения, поэтому при ненулевых stage violations после write нужен cleanup-write. Helper всё еще нуждается в `--diff-report` и diff-only обновлении.

# Следующему

Следующий запуск: SML -> preflight -> write helper -> dry-run -> compact audit. Не запускать в первый рабочий день недели.

# Запрос

Пользователь потребовал сейчас закрыть проблемы automation-3: конверсия должна быть проставлена, данные внесены в таблицу, ничего не переносить на следующий запуск.

# Результат

Найден и исправлен ключевой дефект: старый writer automation-3 писал в статические строки `15, 29, 45, 61, 78, 93, 108, 123`; на текущей вкладке `Июнь 2026 ОП` реальные строки `Счет отправлен, %` — `18, 34, 50, 66, 84, 100, 116, 132`. `write_daily.py` переведен на динамический поиск строк внутри блоков менеджеров. Добавлен `run_and_write_daily.ps1`. `calc_invoice_conversion.py` получил `--run-date` и `unknown_assigned_by`.

Пересчитаны и записаны в правильные строки все рабочие target-date июня с `2026-06-01` по `2026-06-19`, включая ранее отсутствовавший target `2026-06-05` в колонке `Q`. Итоговая сверка: `112` ячеек, расхождений нет. Очищены явные следы старого writer: `AE93`, `AE123`. Heartbeat automation-3 обновлен на новый маршрут.

# Проверки

SML ok, preflight ok=true, py_compile ok. Smoke-test `run_and_write_daily.ps1 -RunDate 2026-06-22 -TargetDate 2026-06-19` прошел успешно, writer подтвердил `AE18=33%`, `AE34=0%`, `AE50=0%`, `AE66=25%`, `AE84=50%`, `AE100=40%`, `AE116=100%`, `AE132=67%`.

# Риски

Автоматически очищены только явно ошибочные текущие следы старой записи `AE93` и `AE123`; остальные нецелевые строки не трогались без надежного источника восстановления. За `2026-06-15` есть `unknown_assigned_by`: `user_id=975`, `new_deals=2`.

# Следующему

Использовать только `automations/conversion_invoices/run_and_write_daily.ps1`. Статические строки больше не использовать.

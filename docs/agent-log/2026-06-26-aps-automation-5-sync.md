# Запрос

Зафиксировать результат ежедневной синхронизации АПС `automation-5` Bitrix24 -> Google Sheets за 2026-06-26.

# Результат

АПС запущена, так как 2026-06-26 — пятница и не первый рабочий день недели. SML и preflight прошли успешно.

Основной write helper `C:\Users\koval\Documents\ОК.ру\automations\aps\run_sync_with_env.ps1 -Write`: `since_utc=2026-06-25T06:24:37+00:00`, fetched=386, matched=64, inserted=8, updated=56, deleted=11. После compact audit найдено одно бизнес-расхождение по компании у сделки `136427`; корректирующий write: fetched=10, matched=2, inserted=0, updated=2, deleted=0.

Финальная таблица: 155 строк, 155 уникальных ID, дубли=0, запрещенные менеджеры=0, нарушения воронки=0, нарушения стадии=0, строки к удалению=0, бизнес-расхождения=0. Последние 8 добавленных строк имеют заполненный `Общий смысл`.

# Проверки

Compact audit проверил видимый `Менеджер`, текущего ответственного Bitrix, `CATEGORY_ID`, текущую стадию, missing deals и бизнес-поля. Осталось 21 format-only отличие по суммам из-за пробелов-разделителей.

# Риски

Нужен штатный `--diff-report` и diff-only обновление, чтобы не считать overlap как бизнес-обновления. У сделки `137397` очень короткий `Общий смысл` длиной 7 символов. Gate `trading_mvp` остается RUNNING, но АПС не трогала trading-процессы.

# Следующему

Следующий запуск: SML -> preflight -> write helper -> dry-run -> compact audit. Не запускать в первый рабочий день недели.
